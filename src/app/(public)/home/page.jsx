import React from "react";
import { Button } from "@/components/ui/button";

const page = () => {
  return (
    <div className="h-full w-full flex justify-center items-center">
      <Button className={"rounded-full"}>Home Page</Button>
    </div>
  );
};

export default page;
